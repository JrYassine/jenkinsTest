node {
        stage('Init') {
            checkout scm
            sh "npm --version"
        }

        stage("Test") {
            
            echo 'Testing..'
        }


        stage("Publish api") {
           echo 'Publish ai..'
            
        }
}

